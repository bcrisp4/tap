<!-- The wordmark: lowercase "tap" + Klein-blue period rendered as a
     larger junction dot. -->
<span class="wordmark">tap<span class="dot" aria-hidden="true"></span></span>

<style>
	.wordmark {
		font-family: var(--sans);
		font-weight: 600;
		letter-spacing: -0.02em;
		font-size: 17px;
		color: var(--ink);
		display: inline-flex;
		align-items: baseline;
		gap: 1px;
	}
	.dot {
		display: inline-block;
		width: 5px;
		height: 5px;
		border-radius: 50%;
		background: var(--accent);
		transform: translateY(-1px);
		margin-left: 1px;
	}
</style>
